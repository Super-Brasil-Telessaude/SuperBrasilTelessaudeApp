import { View, Text, TouchableOpacity } from "react-native";

export default function ChooseRole() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-2xl font-bold mb-10">Quem é você?</Text>
    </View>
  );
}
